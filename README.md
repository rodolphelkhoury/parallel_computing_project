# Parallel Heat Simulation
- this readme will be changed
## Prerequisites
- C++ compiler with C++17 support
- CMake ≥ 3.16
- Ninja (or any CMake-supported build system)
- (Optional) MSYS2 on Windows

## Build Instructions
cd parallel_computing_project
mkdir -p build
cd build
cmake ..
cmake --build .

The executable `heat_solver.exe` will be created in the `build` directory.

## Run Instructions
./heat_solver.exe
